node_modules
dist
.vite
# SQLite DBs (may contain credentials, test data, app state — do not commit)
*.db*
db_backup
uploads
# Wiki markdown and metadata — per-environment; do not merge from dev/main
content/wiki/
.env
*.log
.DS_Store

# Vite temp config (transpiled from vite.config.ts)
vite.config.ts.timestamp-*.mjs
