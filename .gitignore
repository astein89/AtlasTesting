node_modules
dist
# Generated by scripts/write-build-version.mjs (commit at build / dev start)
build-version.json
.vite
# SQLite DBs (may contain credentials, test data, app state — do not commit)
*.db*
db_backup
uploads
# Wiki markdown and metadata — per-environment; do not merge from dev/main
# Live wiki data (editable on disk); canonical defaults live in content/wiki-seed/
content/wiki/
.env
config.json
*.log
.DS_Store

# Vite temp config (transpiled from vite.config.ts)
vite.config.ts.timestamp-*.mjs

# AMR emulator persisted runtime state and tunable settings (per-developer)
scripts/amr-emulator-ui/sim-settings.json
scripts/amr-emulator-ui/emulator-state.json
scripts/amr-emulator-ui/emulator-state.bak.json
scripts/amr-emulator-ui/emulator-state.port-*.json
scripts/amr-emulator-ui/emulator-state.port-*.bak.json
scripts/amr-emulator-ui/emulator-state.corrupt.*
