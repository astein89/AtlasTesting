# Getting started

This page lives at path `guides/getting-started` (nested under `guides/`).

## Mermaid example

```mermaid
flowchart LR
  A[Wiki] --> B[Markdown]
  B --> C[Mermaid]
```

Return to the [wiki index](/wiki/index).
